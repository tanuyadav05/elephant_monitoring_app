import React from 'react';
import { View, Text } from 'react-native';

const AlertsScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Alerts Page</Text>
      {/* You can map through alerts data here */}
    </View>
  );
};

export default AlertsScreen;